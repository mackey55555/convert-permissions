on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    name: Greeting
    steps:
      - name: Hello
        uses: mackey55555/asana-github-plugin@v1.0.0
        with:
          asana-secret: ${{ secrets.ASANA_SECRET }}
          milliseconds: 1002
        # with: