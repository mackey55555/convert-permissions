on: [push]

jobs:
  hello-world:
    runs-on: ubuntu-latest
    name: Greeting
    steps:
      - name: Hello
        uses: mackey55555/github-asana-plugin@main
        with:
          asana-secret: ${{ secrets.ASANA_SECRET }}